var Sent1 = "This is the beginning of the string,"; // Assigning a string value to the variable Sent1
var Sent2 = " and this is the end of the string.";  // Assigning a string value to the variable Sent2

var concatSent = (Sent1 + Sent2);       // Concatenating the two strings Sent1 and Sent2
var daysWithoutAccident = (50 - 50);    // Here is a line of code with an expression

window.alert("This page has gone " + daysWithoutAccident + " days without an accident."); // Using window.alert to display a string and a variable
document.write(concatSent);     // Using the document.write alert to write out our concatenated string from earlier.